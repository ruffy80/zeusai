# Unicorn public-discovery snippet (inline serve, no upstream dependency)
# Included from /etc/nginx/sites-available/unicorn (managed by deploy step
# `Patch nginx public-discovery`) inside the active server blocks for
# zeusai.pro / www.zeusai.pro.
#
# These endpoints are served INLINE because:
#   1. The deployed nginx config does not declare an `unicorn_site` upstream
#      (previous proxy version failed nginx -t with "host not found").
#   2. The payloads are small/static and identical to backend/modules/polish-pack.
#   3. The `^~` / `=` modifiers guarantee priority over any default
#      `location ~ /\.` deny regex that was returning bare 403 for the
#      /.well-known/* and /humans.txt paths.

# NOTE: /.well-known/acme-challenge/ is intentionally NOT redefined here —
# the existing /etc/nginx/sites-available/unicorn already declares it (and
# nginx errors with `duplicate location` if we redeclare). This snippet only
# adds the public-discovery endpoints that were missing.

# Reject credential/config probes before they reach SSR/API fallbacks. Exact
# `/.well-known/*` locations below retain priority and remain public.
location ~* "(?:^|/)\.(?!well-known(?:/|$))(?:env|git|aws|ssh|svn|hg)(?:$|/|\.)" {
    access_log off;
    add_header Cache-Control "no-store" always;
    return 404;
}
location ~* "(?:^|/)(?:wp-config\.php|composer\.(?:json|lock)|package-lock\.json)(?:$|/)" {
    access_log off;
    add_header Cache-Control "no-store" always;
    return 404;
}

# ── /.well-known/security.txt (RFC 9116) ──────────────────────────────────
location = /.well-known/security.txt {
    default_type "text/plain; charset=utf-8";
    add_header X-Polish-Pack "nginx-inline" always;
    add_header Cache-Control "public, max-age=3600" always;
    return 200 "Contact: mailto:vladoi_ionut@yahoo.com\nContact: https://zeusai.pro/security\nExpires: 2027-12-31T00:00:00.000Z\nPreferred-Languages: en, ro\nCanonical: https://zeusai.pro/.well-known/security.txt\nPolicy: https://zeusai.pro/security\nAcknowledgments: https://zeusai.pro/trust\n# Responsible disclosure: 90-day coordinated disclosure window.\n# We sign all advisories with Ed25519 (see /.well-known/did.json).\n";
}

location = /security.txt {
    default_type "text/plain; charset=utf-8";
    add_header X-Polish-Pack "nginx-inline" always;
    add_header Cache-Control "public, max-age=3600" always;
    return 200 "Contact: mailto:vladoi_ionut@yahoo.com\nContact: https://zeusai.pro/security\nExpires: 2027-12-31T00:00:00.000Z\nPreferred-Languages: en, ro\nCanonical: https://zeusai.pro/.well-known/security.txt\nPolicy: https://zeusai.pro/security\nAcknowledgments: https://zeusai.pro/trust\n# Responsible disclosure: 90-day coordinated disclosure window.\n# We sign all advisories with Ed25519 (see /.well-known/did.json).\n";
}

# ── /humans.txt ───────────────────────────────────────────────────────────
location = /humans.txt {
    default_type "text/plain; charset=utf-8";
    add_header X-Polish-Pack "nginx-inline" always;
    add_header Cache-Control "public, max-age=3600" always;
    return 200 "/* TEAM */\n  Owner & Architect: Vladoi Ionut\n  Contact: vladoi_ionut@yahoo.com\n  Site: https://zeusai.pro\n\n/* THANKS */\n  To every autonomous agent that bought, audited, or improved ZeusAI.\n\n/* SITE */\n  Built on: Node.js, plain HTTP, Express\n  Standards: RFC 9116, W3C Trace Context, RFC 6962, CycloneDX 1.5\n  Doctrine: sovereignty, signed outcomes, additive evolution.\n";
}

# ── /.well-known/autonomy.json — TAOS/1.0 live score (backend-owned) ────────
# The backend (port 3000) serves this at ['/api/autonomy/os', '/.well-known/autonomy.json'].
# The generic `^~ /.well-known/` block routes to unicorn_site (3001) which does
# not proxy it — this exact-match location overrides that and sends it to the backend.
location = /.well-known/autonomy.json {
    proxy_pass http://127.0.0.1:3000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    add_header Cache-Control "no-store" always;
}

# ── /.well-known/neural-autonomy.json — NAOS/1.0 composition score ─────────
# Backend serves ['/api/autonomy/neural', '/.well-known/neural-autonomy.json'].
# Exact-match beats generic ^~ /.well-known/ (which may 403 on unknown files).
location = /.well-known/neural-autonomy.json {
    proxy_pass http://127.0.0.1:3000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    add_header Cache-Control "no-store" always;
}

# ── /.well-known/autonomy-bond.json — SUBOS/1.0 site↔unicorn bond ──────────
location = /.well-known/autonomy-bond.json {
    proxy_pass http://127.0.0.1:3000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    add_header Cache-Control "no-store" always;
}

# ── /.well-known/triad-bond.json — TBOS/1.0 triad never-down ───────────────
location = /.well-known/triad-bond.json {
    proxy_pass http://127.0.0.1:3000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    add_header Cache-Control "no-store" always;
}

# ── /.well-known/commerce-bond.json — CBLOS/1.0 site↔backend commerce truth ─
location = /.well-known/commerce-bond.json {
    proxy_pass http://127.0.0.1:3000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    add_header Cache-Control "no-store" always;
}

# ── /.well-known/brand-spectrum.json — CIC/1.0 chromatic continuum ─────────
location = /.well-known/brand-spectrum.json {
    proxy_pass http://127.0.0.1:3000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    add_header Cache-Control "public, max-age=60" always;
}

# ── /.well-known/world-dropship.json — WDOS/1.0 permanent world feed ───────
location = /.well-known/world-dropship.json {
    proxy_pass http://127.0.0.1:3001;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    add_header Cache-Control "public, max-age=30" always;
}

# ── /.well-known/module-reality.json — MRCOS/1.0 module reality audit ──────
location = /.well-known/module-reality.json {
    proxy_pass http://127.0.0.1:3000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    add_header Cache-Control "public, max-age=60" always;
}

# ── /.well-known/clos.json — CLOS/1.0 Closed-Loop Commerce OS ──────────────
location = /.well-known/clos.json {
    proxy_pass http://127.0.0.1:3000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    add_header Cache-Control "public, max-age=15" always;
}

# ── /.well-known/aacos.json — AACOS/1.0 Autonomy Action Continuum ──────────
location = /.well-known/aacos.json {
    proxy_pass http://127.0.0.1:3000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    add_header Cache-Control "public, max-age=10" always;
}

# ── /.well-known/rocs.json — ROCS/1.0 Reality Ops Continuum ────────────────
location = /.well-known/rocs.json {
    proxy_pass http://127.0.0.1:3000/api/rocs/status;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    add_header Cache-Control "public, max-age=10" always;
}

# ── /.well-known/agde.json — WGC/1.0 World Gravity Continuum ───────────────
location = /.well-known/agde.json {
    proxy_pass http://127.0.0.1:3000/api/agde/status;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    add_header Cache-Control "public, max-age=10" always;
}

# ── /.well-known/immortality.json — ICP/1.0 Immortality Continuum ─────────
location = /.well-known/immortality.json {
    proxy_pass http://127.0.0.1:3000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    add_header Cache-Control "no-store" always;
}

# ── /.well-known/continuity.json — CAC/1.0 Continuity Attestation Chain ───
location = /.well-known/continuity.json {
    proxy_pass http://127.0.0.1:3000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    add_header Cache-Control "no-store" always;
}

# ── /.well-known/merchant.json — MTS/1.0 Merchant Trust Standard ──────────
location = /.well-known/merchant.json {
    proxy_pass http://127.0.0.1:3000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    add_header Cache-Control "no-store" always;
}

# ── /.well-known/agents.json (AI agent discovery) ─────────────────────────
location = /.well-known/agents.json {
    default_type "application/json; charset=utf-8";
    add_header X-Polish-Pack "nginx-inline" always;
    add_header Cache-Control "public, max-age=300" always;
    return 200 '{"version":"1.0","name":"ZeusAI Unicorn Platform","owner":"Vladoi Ionut","contact":"vladoi_ionut@yahoo.com","site":"https://zeusai.pro","endpoints":{"api":"https://zeusai.pro/api","health":"https://zeusai.pro/health","catalog":"https://zeusai.pro/api/catalog","openapi":"https://zeusai.pro/openapi.json","did":"https://zeusai.pro/.well-known/did.json","integrity":"https://zeusai.pro/.well-known/unicorn-integrity.json","security":"https://zeusai.pro/.well-known/security.txt","pomx":"https://zeusai.pro/.well-known/pomx.json","exchange":"https://zeusai.pro/api/pomx/exchange"},"capabilities":["autonomous-commerce","proof-of-margin-exchange","viral-engine","payment-orchestration","carbon-accounting","quantum-security","signed-receipts"],"discovery":["/.well-known/did.json","/.well-known/ai-attestation","/.well-known/unicorn-integrity.json","/.well-known/pomx.json","/agents.json","/openapi.json"]}';
}

# ── /.well-known/zeusai.json — platform discovery (must never 403 via /root alias)
location = /.well-known/zeusai.json {
    proxy_pass http://127.0.0.1:3001;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    add_header Cache-Control "public, max-age=300" always;
}

# ── /.well-known/pomx.json — Proof-of-Margin Exchange (PoMX/1.0) ──────────
# Exact location must beat the generic ^~ /.well-known/ filesystem alias.
location = /.well-known/pomx.json {
    proxy_pass http://127.0.0.1:3001;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-CSP-Nonce $request_id;
    add_header Cache-Control "public, max-age=60" always;
}

# ── /.well-known/wacp.json — World AI Commerce Protocol ───────────────────
location = /.well-known/wacp.json {
    proxy_pass http://127.0.0.1:3001;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-CSP-Nonce $request_id;
    add_header Cache-Control "public, max-age=60" always;
}

# ── /.well-known/eop.json — Earth Outcome Protocol (EOP/1.0) ──────────────
location = /.well-known/eop.json {
    proxy_pass http://127.0.0.1:3001;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-CSP-Nonce $request_id;
    add_header Cache-Control "public, max-age=60" always;
}

# ── /.well-known/pre-keys.json — Pre-Keys Activation (PKA/1.0) ─────────────
location = /.well-known/pre-keys.json {
    proxy_pass http://127.0.0.1:3001;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-CSP-Nonce $request_id;
    add_header Cache-Control "no-store" always;
}

# ── /api/pre-keys* + telegram bind-status + lightning (site-owned) ─────────
location ^~ /api/pre-keys {
    proxy_pass http://127.0.0.1:3001;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
location = /api/telegram/bind-status {
    proxy_pass http://127.0.0.1:3001;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
location ^~ /api/lightning {
    proxy_pass http://127.0.0.1:3001;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}

# ── /api/eop* — Earth Outcome Protocol (GET + POST mint/verify) ───────────
# Must precede generic location ^~ /api/ (backend:3000). Site owns EOP surface.
location ^~ /api/eop {
    proxy_pass http://127.0.0.1:3001;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 60s;
    client_max_body_size 2m;
}

# ── /api/pomx* — Proof-of-Margin Exchange ─────────────────────────────────
location ^~ /api/pomx {
    proxy_pass http://127.0.0.1:3000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 60s;
    client_max_body_size 2m;
}

# ── /.well-known/zeusai-key.{pub,pem} — forever Ed25519 site-sign key ─────
# Stable across deploys; persisted in /var/www/unicorn/shared/site-sign.pem.
# Forwarded to the site (3001) which knows how to read both PEM/JWK forms.
location = /.well-known/zeusai-key.pub {
    proxy_pass http://127.0.0.1:3001;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-Proto $scheme;
    add_header X-Polish-Pack "forever-key" always;
    add_header Cache-Control "public, max-age=300" always;
}
location = /.well-known/zeusai-pubkey {
    proxy_pass http://127.0.0.1:3001;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-Proto $scheme;
    add_header X-Polish-Pack "forever-key" always;
    add_header Cache-Control "public, max-age=300" always;
}
# ── /.well-known/telegram-profit-group.json — TPG/1.0 ─────────────────────
location = /.well-known/telegram-profit-group.json {
    proxy_pass http://127.0.0.1:3001;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    add_header Cache-Control "no-store" always;
}
location ^~ /api/telegram/ {
    proxy_pass http://127.0.0.1:3001;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
