# nginx-unicorn.conf — OPTIMIZED: Zero Downtime + HTTP/2 + HSTS + Hardening
# Unicorn AI — Nginx config completa (multi-subdomain, rate limiting, security)
# Suporta: zeusai.pro, www.zeusai.pro, api.zeusai.pro, orchestrator.zeusai.pro, *.zeusai.pro
# Copiaza in /etc/nginx/sites-available/unicorn si activeaza cu:
#   ln -sf /etc/nginx/sites-available/unicorn /etc/nginx/sites-enabled/unicorn
#   nginx -t && systemctl reload nginx
# SSL este configurat automat de certbot --nginx.
# HTTP/2 este activat automat pe blocurile HTTPS adaugate de certbot.

# ── Upstream backend cu health check pasiv (Zero Downtime) ───────────────────
upstream unicorn_backend {
    server 127.0.0.1:3000;
    keepalive 64;                       # conexiuni persistente catre backend
}

# ── Upstream site (unicorn-site, PM2 cluster on port 3001) ───────────────────
# SSR layer (src/site/v2/shell.js, template.js) — JSON-LD, hreflang, preload
# headers, polish-pack assets. Backed by unicorn_backend as fallback (backup)
# so a site outage degrades to the backend's getSiteHtml() instead of 502.
# Public traffic for `/`, `/stream`, static assets is routed here while every
# `/api/*` and `/internal/*` stays on unicorn_backend (single source of truth
# for SQLite, ledgers, orchestrators).
upstream unicorn_site {
    server 127.0.0.1:3001       max_fails=3 fail_timeout=10s;
    server 127.0.0.1:3000 backup;       # graceful fallback when site is down
    keepalive 32;
}

# Safe text compression when this file is included in nginx's http context.
# Brotli is intentionally not enabled here because many Ubuntu nginx builds do
# not ship the module; gzip is built-in and keeps reloads deterministic.
gzip on;
gzip_vary on;
gzip_proxied any;
gzip_min_length 1024;
gzip_comp_level 5;
gzip_types text/plain text/css text/xml application/json application/javascript application/xml application/xml+rss image/svg+xml;

# Mapeaza header-ul Connection pentru WebSocket vs HTTP obisnuit
map $http_upgrade $connection_upgrade {
    default upgrade;
    ""      close;
}

# ── Microcaching pentru răspunsuri API dinamice (reduce latenta 10-100x) ─────
proxy_cache_path /var/cache/nginx/unicorn
    levels=1:2
    keys_zone=unicorn_api_cache:10m
    max_size=500m
    inactive=10m
    use_temp_path=off;

# Rate limiting zones — îmbunătățite
limit_req_zone $binary_remote_addr zone=api:10m     rate=60r/m;
limit_req_zone $binary_remote_addr zone=auth:10m    rate=10r/m;
limit_req_zone $binary_remote_addr zone=general:10m rate=120r/m;
limit_req_zone $binary_remote_addr zone=ai:10m      rate=20r/m;
limit_conn_zone $binary_remote_addr zone=conn_limit:10m;

# ── Default server: IP-based requests (health checks from CI, load balancers) ──
# Handles requests that don't match any server_name (e.g. http://SERVER_IP/health)
# Returns 200 for /health and /api/health; proxies everything else to backend.
server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;

    # Allow certbot ACME challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
        default_type "text/plain";
    }

    # Health check endpoint — returns 200 directly (no redirect) for monitoring
    location = /health {
        proxy_pass         http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_connect_timeout 10s;
        proxy_send_timeout    10s;
        proxy_read_timeout    10s;
    }

    location = /api/health {
        proxy_pass         http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_connect_timeout 10s;
        proxy_send_timeout    10s;
        proxy_read_timeout    10s;
    }

    # All other IP-based requests → proxy to backend
    # (redirect to HTTPS zeusai.pro would fail if SSL cert not yet obtained)
    location / {
        proxy_pass         http://unicorn_backend;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_set_header   Upgrade           $http_upgrade;
        proxy_set_header   Connection        $connection_upgrade;
        proxy_connect_timeout 60s;
        proxy_send_timeout    60s;
        proxy_read_timeout    60s;
    }
}

# ── Wildcard catch-all: orice *.zeusai.pro necunoscut → redirect spre main ────
# Aceasta trebuie sa fie dupa default_server (subdomeniile explicite au prioritate)
server {
    listen 80;
    listen [::]:80;
    server_name *.zeusai.pro;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
        default_type "text/plain";
    }

    return 301 https://zeusai.pro$request_uri;
}

# ── zeusai.pro + www: proxy direct la backend (HTTP funcțional fără SSL) ──────
# Certbot --nginx va adăuga automat blocul return 301 https://...
# după ce certificatul SSL este emis. Până atunci site-ul este accesibil pe HTTP.
server {
    listen 80;
    listen [::]:80;
    server_name zeusai.pro www.zeusai.pro;

    # ── NEVER-502 fallback ───────────────────────────────────────────────
    # When both unicorn-site:3001 and unicorn-backend:3000 are down, serve
    # a static auto-refreshing maintenance page from /var/www/maintenance/
    # instead of nginx's default 502. Installed by
    # scripts/install-maintenance-page.sh on every deploy. The named
    # location is `internal` so it cannot be hit directly.
    # NOTE the `=503` before the named location: without it, `error_page ... =
    # @location` REWRITES the upstream 5xx to HTTP 200, so browsers, CDNs and
    # uptime monitors cached/treated the maintenance page as a successful
    # response. Forcing =503 keeps the real "Service Unavailable" status while
    # still serving the friendly static page.
    error_page 500 502 503 504 =503 @zeus_maintenance;
    location @zeus_maintenance {
        internal;
        root /var/www/maintenance;
        try_files /index.html =503;
        default_type "text/html; charset=utf-8";
        add_header Cache-Control "no-store, no-cache, must-revalidate" always;
        add_header Retry-After "30" always;
        add_header X-Zeus-Maintenance "1" always;
    }

    # Permite certbot ACME challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
        default_type "text/plain";
    }

    location = /health {
        proxy_pass         http://unicorn_backend;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_connect_timeout 10s;
        proxy_send_timeout    10s;
        proxy_read_timeout    10s;
        access_log off;
    }

    location = /robots.txt {
        proxy_pass         http://unicorn_site;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_next_upstream error timeout http_502 http_503 http_504;
    }

    location = /sitemap.xml {
        proxy_pass         http://unicorn_site;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_next_upstream error timeout http_502 http_503 http_504;
    }

    # Polish-pack public-discovery endpoints (humans.txt, offline.html,
    # agents.json, ai-plugin.json, mcp.json, did.json) — explicit exact-match
    # locations so they bypass any `location ~ /\.` deny regex and any
    # implicit `try_files $uri =404` rule from a default site config.
    location = /humans.txt           { proxy_pass http://unicorn_site; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; }
    location = /offline.html         { proxy_pass http://unicorn_site; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; }
    location = /agents.json          { proxy_pass http://unicorn_site; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; }

    # ── Concierge AI chat (POST /api/concierge[/stream|/feedback]) ──────────
    # Implemented exclusively in `unicorn_site` (UNICORN_FINAL/src/index.js)
    # because it depends on the SSR layer's portal/uaic/unifiedCatalog/intent
    # composer. We pin these specific paths to the site upstream BEFORE the
    # generic `/api/` rule so they don't 404 on the backend. Backend remains
    # the upstream `backup` so a site outage degrades gracefully (the backend
    # also serves a getSiteHtml() fallback when the site cluster is down).
    # `^~` ensures the prefix wins over any regex location.
    location ^~ /api/concierge {
        proxy_pass         http://unicorn_site;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_set_header   Upgrade           $http_upgrade;
        proxy_set_header   Connection        $connection_upgrade;
        # /api/concierge/stream is SSE — disable buffering, long read timeout.
        proxy_buffering    off;
        proxy_cache        off;
        proxy_connect_timeout 60s;
        proxy_send_timeout    300s;
        proxy_read_timeout    300s;
        proxy_next_upstream error timeout http_502 http_503 http_504;
    }

    # ── Site-only API endpoints (defined in src/index.js, not backend) ─────
    # `location =` exact match has higher priority than `location /api/`, so
    # these reach the unicorn_site upstream while every other /api/* still
    # goes to unicorn_backend. Required for catalog smoke tests which pull
    # the master catalog (built from buildMasterCatalog in src/index.js).
    location = /api/catalog/master {
        proxy_pass         http://unicorn_site;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_next_upstream error timeout http_502 http_503 http_504;
        proxy_connect_timeout 30s;
        proxy_read_timeout    30s;
    }

    # Same catalog truth as storefront — do not serve backend's smaller SaaS seed.
    location = /api/catalog {
        proxy_pass         http://unicorn_site;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_next_upstream error timeout http_502 http_503 http_504;
        proxy_connect_timeout 15s;
        proxy_read_timeout    15s;
        proxy_cache          off;
    }

    location = /api/health/live {
        proxy_pass         http://unicorn_backend;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_connect_timeout 2s;
        proxy_send_timeout    2s;
        proxy_read_timeout    2s;
        access_log off;
        proxy_cache off;
    }

    # ── Live SSE mirrors (Unicorn → browsers) — must not be buffered/cached ──
    # Generic /api/ enables proxy_buffering which breaks EventSource streams.
    location = /api/pricing/live/stream {
        proxy_pass         http://unicorn_backend;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_set_header   Connection        '';
        proxy_buffering    off;
        proxy_cache        off;
        chunked_transfer_encoding off;
        proxy_read_timeout 3600s;
        add_header         X-Accel-Buffering no;
    }
    # Site BFF modules cache (auth-free mirror) — must NOT hit backend /api/modules (401).
    location = /api/modules {
        proxy_pass         http://unicorn_site;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_next_upstream error timeout http_502 http_503 http_504;
        proxy_read_timeout 60s;
    }
    # Site BFF SSE relay for autonomous modules (named event:snapshot with modules[]).
    location = /api/events {
        proxy_pass         http://unicorn_site;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_set_header   Connection        '';
        proxy_buffering    off;
        proxy_cache        off;
        chunked_transfer_encoding off;
        proxy_read_timeout 3600s;
        add_header         X-Accel-Buffering no;
    }
    location = /api/modules/stream {
        proxy_pass         http://unicorn_backend;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_set_header   Connection        '';
        proxy_buffering    off;
        proxy_cache        off;
        chunked_transfer_encoding off;
        proxy_read_timeout 3600s;
        add_header         X-Accel-Buffering no;
    }
    location = /api/unicorn/events {
        proxy_pass         http://unicorn_backend;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_set_header   Connection        '';
        proxy_buffering    off;
        proxy_cache        off;
        chunked_transfer_encoding off;
        proxy_read_timeout 3600s;
        add_header         X-Accel-Buffering no;
    }
    location = /api/catalog/diff {
        proxy_pass         http://unicorn_site;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_next_upstream error timeout http_502 http_503 http_504;
        proxy_connect_timeout 30s;
        proxy_read_timeout    30s;
    }

    # ── Site-served API prefixes (must precede generic /api/) ────────────────
    # These domains live in src/index.js: transparency, referral, leads,
    # innovations/innovation, funnel, trust, dr, services/changed.
    location ^~ /api/transparency    { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    location ^~ /api/referral        { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    location ^~ /api/leads           { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    location ^~ /api/innovations     { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    location ^~ /api/innovation/     { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    # Exact coverage path (defense if prefix rule is missing on an older host).
    location = /api/innovation/coverage { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    # Frontier F1–F12 commerce inventions (src/frontier-engine.js — site-only).
    # Without these, /api/ falls through to backend SPA HTML and /pledge,/gift,/refund break.
    location = /api/frontier/status      { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    location ^~ /api/refund/             { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    location ^~ /api/pledge              { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    location ^~ /api/gift/               { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    location ^~ /api/bandit/             { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    location ^~ /api/cancel/             { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    location ^~ /api/discount/           { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    location ^~ /api/email/proof         { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    # Exact Frontier outcome/carbon paths ONLY — do NOT use ^~ /api/outcome/ or
    # ^~ /api/carbon/ prefixes (those shadow backend ledger + auth-gated carbon trading).
    location = /api/outcome/list         { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    location = /api/outcome/anchor       { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    location = /api/carbon/cart          { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    location ^~ /api/receipt/nft/        { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    location = /api/checkout/cascade    { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    # 100Y innovation + performance contracts live on the site process.
    location ^~ /api/v100/               { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    location ^~ /api/funnel/         { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    location ^~ /api/trust/          { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    location ^~ /api/dr/             { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    location = /api/services/changed { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    location = /api/services         { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    # MUST match /api/services — homepage Sync Drift + api-aliases compare these.
    # Without this pin, generic ^~ /api/ sends list to backend (different catalog
    # → false "265 mismatch" on the Control Tower panel).
    location = /api/services/list    { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }

    # ── Sovereign commerce (BTC checkout, watcher, delivery) — site-only ────
    # Implemented in src/site/sovereign-commerce.js and dispatched by
    # unicornHandler in src/index.js. Must reach `unicorn_site`, otherwise
    # the default `/api/` rule sends them to `unicorn_backend` which does not
    # implement them and returns 404. Production exhibited this as 404 on
    # /api/commerce/{health,price,recent-sales} and /api/checkout/create.
    location = /api/checkout/create     { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    # BIP-21 QR for sovereign invoices — MUST hit site (sovereign-commerce.js).
    # Regex pins LOSE to `location ^~ /api/` (backend SPA HTML → empty QR).
    # Prefix ^~ /api/checkout/ord_ is longer than ^~ /api/, so it wins, while
    # leaving /api/checkout/create|cascade|recovery on their own exact/backend pins.
    # Preferred public QR path is /checkout/:id/qr.svg (also under ^~ /checkout/).
    location ^~ /api/checkout/ord_ {
      proxy_pass http://unicorn_site;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
      proxy_next_upstream error timeout http_502 http_503 http_504;
      proxy_read_timeout 30s;
    }
    # Generic QR encoder used by checkout canvas / invoice fallbacks.
    location = /api/qr {
      proxy_pass http://unicorn_site;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
      proxy_next_upstream error timeout http_502 http_503 http_504;
      proxy_read_timeout 30s;
    }
    location = /api/commerce/health     { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    location = /api/commerce/price      { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    location = /api/commerce/recent-sales { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    location = /api/commerce/integrity  { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    location = /api/commerce/metrics    { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    location = /api/commerce/reconcile  { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    location = /api/commerce/funnel     { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    # Public verification keys (Trust Center). Exact match beats any deny-dotfile regex.
    location = /.well-known/keys.json   { proxy_pass http://unicorn_backend; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 30s; }
    # Legacy status alias — never 404 behind nginx *.html static rules.
    location = /unicorn-status.html     { return 301 /status; }
    location ^~ /api/order/             { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    location ^~ /api/entitlements/      { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    # Checkout chooser + invoices. If a server-level `add_header Content-Security-Policy`
    # exists (live zeusai.conf), this location MUST set its own add_header so nginx
    # does not inherit a second CSP with a different nonce — dual CSP blocks app.js
    # and payment buttons appear dead. Pass X-CSP-Nonce so site script nonces match.
    location ^~ /checkout/ {
      add_header Cache-Control "no-cache, no-store, must-revalidate" always;
      proxy_pass http://unicorn_site;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
      proxy_set_header X-CSP-Nonce $request_id;
      proxy_next_upstream error timeout http_502 http_503 http_504;
      proxy_read_timeout 60s;
    }

    # ── Admin auth session (cookie-based HMAC — lives in src/index.js) ─────────
    # /api/admin/login, /api/admin/logout, /api/admin/session are handled ONLY
    # by unicorn-site (src/index.js:3606-3632). They MUST NOT go to the backend
    # which has no such routes — omitting these rules caused the admin panel to
    # show "ADMIN_SECRET not configured" and block the owner from logging in.
    location = /api/admin/login    { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 30s; }
    location = /api/admin/logout   { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 30s; }
    location = /api/admin/session  { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 30s; }

    # ── Admin site-only commerce / config / status operations ────────────────
    # These admin endpoints are implemented in src/index.js (not the backend):
    # commerce (order review, delivery, refund), config, test-email, status,
    # fulfill-order, owner-revenue. Must reach unicorn_site.
    location ^~ /api/admin/commerce    { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 60s; }
    location = /api/admin/config       { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 30s; }
    location = /api/admin/test-email   { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 30s; }
    location = /api/admin/status       { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 30s; }
    location = /api/admin/fulfill-order { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 30s; }
    location = /api/admin/owner-revenue { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 30s; }

    # ── Cryptoauth (Ed25519 passwordless) — resilient, no single point of failure ──
    # The challenge store is now CROSS-PROCESS DURABLE (data/cryptoauth/challenges.json,
    # atomic writes) and shared by every process that mounts the cryptoauth module
    # (the backend fork AND every site cluster worker read/write the same file).
    # Account operations (create / login / recover) therefore no longer have to be
    # pinned to a single backend instance — routing to the unicorn_site cluster is
    # safe (challenges no longer split across workers) and removes the hard
    # "server not responding" that every backend restart/OOM used to cause.
    # unicorn_site lists 127.0.0.1:3000 as `backup`, so if the whole site cluster
    # is down nginx still falls back to the backend, which serves auth locally too.
    location ^~ /api/cryptoauth/ { proxy_pass http://unicorn_site; proxy_http_version 1.1; proxy_set_header Host $host; proxy_set_header X-Real-IP $remote_addr; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto $scheme; proxy_next_upstream error timeout http_502 http_503 http_504; proxy_read_timeout 30s; }

    # ── Backend (source of truth) — APIs, internal endpoints, .well-known ────
    # Everything that touches SQLite, ledgers, orchestrators, audit logs MUST
    # stay on unicorn_backend (singleton fork-mode). The site cluster only
    # serves SSR HTML/SSE and proxies APIs through itself when needed.
    # 24/7 zero-downtime contract: tighten retry + microcache + stale-on-error.
    # During PM2 restarts / brief backend hiccups, nginx must NOT surface a raw
    # 502 to the client. With `proxy_cache_use_stale`, GET responses cached in
    # the last few minutes are served while the upstream recovers. Authenticated
    # requests (Authorization header or any Cookie) bypass the cache entirely so
    # we never leak per-user data across sessions. Mutating verbs (POST/PUT/…)
    # are not cached because `proxy_cache_methods` defaults to GET HEAD only.
    location /api/ {
        proxy_pass         http://unicorn_backend;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_set_header   Upgrade           $http_upgrade;
        proxy_set_header   Connection        $connection_upgrade;
        proxy_connect_timeout 60s;
        proxy_send_timeout    60s;
        proxy_read_timeout    60s;
        proxy_next_upstream  error timeout http_502 http_503 http_504;
        proxy_next_upstream_tries   2;
        proxy_next_upstream_timeout 5s;
        proxy_cache           unicorn_api_cache;
        proxy_cache_methods   GET HEAD;
        proxy_cache_valid     200 5s;
        proxy_cache_bypass    $http_authorization $http_cookie $http_cache_control;
        proxy_no_cache        $http_authorization $http_cookie $http_cache_control;
        proxy_cache_use_stale error timeout updating http_500 http_502 http_503 http_504;
        proxy_cache_lock      on;
        proxy_cache_lock_timeout 5s;
        add_header            X-Cache-Status $upstream_cache_status always;
    }

    location /internal/ {
        proxy_pass         http://unicorn_backend;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_connect_timeout 60s;
        proxy_send_timeout    60s;
        proxy_read_timeout    60s;
        proxy_next_upstream  error timeout http_502 http_503 http_504;
        proxy_next_upstream_tries   2;
        proxy_next_upstream_timeout 5s;
    }

    # ── /.well-known/* and dotfile-prefixed public artifacts ────────────────
    # `^~` forces this prefix match to win over any regex `location ~ /\.`
    # deny rule that may exist in /etc/nginx/nginx.conf (the classic hardening
    # snippet that blocks all dotfiles also blocks /.well-known/* — we override
    # it by using a non-regex prefix with `^~`). Routed to unicorn_site because
    # polish-pack (security.txt, agents.json) and the SSR layer live there;
    # the site falls back to unicorn_backend automatically (upstream `backup`).
    # Backend-served discovery docs. An exact `=` match always beats the `^~`
    # prefix catch-all below regardless of source order, but they are pinned
    # BEFORE the catch-all for clarity. Same headers as unicorn-integrity.json.
    location = /.well-known/enterprise.json {
        proxy_pass         http://unicorn_backend;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    location = /.well-known/platform.json {
        proxy_pass         http://unicorn_backend;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    location = /.well-known/neural-autonomy.json {
        proxy_pass         http://unicorn_backend;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    location = /.well-known/autonomy-bond.json {
        proxy_pass         http://unicorn_backend;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    # Forever-key MUST be exact-match before ^~ /.well-known/ and before any
    # global `location ~ /\.` deny — otherwise nginx returns bare 403 HTML.
    location = /.well-known/zeusai-key.pub {
        proxy_pass         http://unicorn_site;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    location = /.well-known/zeusai-pubkey {
        proxy_pass         http://unicorn_site;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    location = /.well-known/triad-bond.json {
        proxy_pass         http://unicorn_backend;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    location = /.well-known/commerce-bond.json {
        proxy_pass         http://unicorn_backend;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    location = /.well-known/origin-gravity.json {
        proxy_pass         http://unicorn_backend;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    location = /.well-known/brand-spectrum.json {
        proxy_pass         http://unicorn_backend;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    location = /.well-known/world-dropship.json {
        proxy_pass         http://unicorn_backend;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    location = /.well-known/module-reality.json {
        proxy_pass         http://unicorn_backend;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    location = /.well-known/clos.json {
        proxy_pass         http://unicorn_backend;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    location = /.well-known/aacos.json {
        proxy_pass         http://unicorn_backend;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    location = /.well-known/rocs.json {
        proxy_pass         http://unicorn_backend/api/rocs/status;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    location = /.well-known/agde.json {
        proxy_pass         http://unicorn_backend/api/agde/status;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        add_header         Cache-Control "public, max-age=10" always;
    }

    location = /.well-known/immortality.json {
        proxy_pass         http://unicorn_backend;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        add_header         Cache-Control "no-store" always;
    }

    location = /.well-known/continuity.json {
        proxy_pass         http://unicorn_backend;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        add_header         Cache-Control "no-store" always;
    }

    location = /.well-known/merchant.json {
        proxy_pass         http://unicorn_backend;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        add_header         Cache-Control "no-store" always;
    }

    location ^~ /.well-known/ {
        proxy_pass         http://unicorn_site;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    location = /.well-known/unicorn-integrity.json {
        proxy_pass         http://unicorn_site;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    # ACME challenge stays on filesystem (certbot writes here).
    # `^~` is critical: without it, a regex `location ~ /\.` deny rule in the
    # global config would catch /.well-known/acme-challenge/ first and break
    # cert renewals.
    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
        default_type "text/plain";
    }

    location /stream {
        proxy_pass         http://unicorn_site;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_set_header   Connection        '';
        proxy_buffering    off;
        proxy_cache        off;
        proxy_read_timeout 3600s;
        chunked_transfer_encoding on;
        proxy_next_upstream error timeout http_502 http_503 http_504;
    }

    location = /snapshot {
        proxy_pass         http://unicorn_site;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_next_upstream error timeout http_502 http_503 http_504;
    }

    # ── Static assets served by unicorn-site (SSR layer) ─────────────────────
    # Cache-Control headers come from the upstream (immutable for hashed assets,
    # no-store for HTML). Nginx microcache helps for the unhashed entry points.
    location /assets/ {
        proxy_pass         http://unicorn_site;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_next_upstream error timeout http_502 http_503 http_504;
    }

    location /icons/ {
        proxy_pass         http://unicorn_site;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_next_upstream error timeout http_502 http_503 http_504;
    }

    location = /manifest.json {
        proxy_pass         http://unicorn_site;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_next_upstream error timeout http_502 http_503 http_504;
    }

    # PWA web manifest alias (some browsers/tools expect .webmanifest extension).
    # Without this, a global hardening rule denies *.webmanifest at 403.
    location = /manifest.webmanifest {
        proxy_pass         http://unicorn_site;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_next_upstream error timeout http_502 http_503 http_504;
    }
    location = /site.webmanifest {
        proxy_pass         http://unicorn_site;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_next_upstream error timeout http_502 http_503 http_504;
    }

    location = /sw.js {
        proxy_pass         http://unicorn_site;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_next_upstream error timeout http_502 http_503 http_504;
    }

    location / {
        proxy_pass         http://unicorn_site;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_set_header   Upgrade           $http_upgrade;
        proxy_set_header   Connection        $connection_upgrade;
        proxy_connect_timeout 60s;
        proxy_send_timeout    60s;
        proxy_read_timeout    60s;
        proxy_next_upstream  error timeout http_502 http_503 http_504;
        proxy_cache           unicorn_api_cache;
        proxy_cache_valid     200 1m;
        proxy_cache_use_stale error timeout updating http_500 http_502 http_503 http_504;
        proxy_cache_bypass    $http_cache_control;
        add_header            X-Cache-Status $upstream_cache_status;
    }

    location ~* \.html$ {
        proxy_pass         http://unicorn_site;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_next_upstream error timeout http_502 http_503 http_504;
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
        add_header Pragma "no-cache" always;
        add_header Expires "0" always;
    }

    location ~* \.(css|js|png|jpg|jpeg|gif|ico|svg|woff2)$ {
        proxy_pass         http://unicorn_site;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_next_upstream error timeout http_502 http_503 http_504;
        expires 1y;
        add_header Cache-Control "public, immutable" always;
    }
}

# ── HTTPS (port 443) — certbot --nginx adaugă automat blocurile server SSL ──────────
# După rularea certbot, aceasta fișier va fi modificat să includă blocuri:
#   server { listen 443 ssl; server_name zeusai.pro ...; ssl_certificate ...; ... }
#   server { listen 443 ssl; server_name api.zeusai.pro ...; ... }
#   server { listen 443 ssl; server_name orchestrator.zeusai.pro ...; ... }
# NU adăuga listen 443 ssl fără ssl_certificate — nginx refuză să pornească!

# ── api.zeusai.pro — subdomain dedicat pentru API ─────────────────────────────
server {
    listen 80;
    listen [::]:80;
    server_name api.zeusai.pro;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
        default_type "text/plain";
    }

    location / {
        proxy_pass         http://unicorn_backend;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_connect_timeout 60s;
        proxy_send_timeout    60s;
        proxy_read_timeout    60s;
    }
}

# ── orchestrator.zeusai.pro — subdomain pentru orchestrator ───────────────────
server {
    listen 80;
    listen [::]:80;
    server_name orchestrator.zeusai.pro;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
        default_type "text/plain";
    }

    location / {
        proxy_pass         http://unicorn_backend;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_connect_timeout 60s;
        proxy_send_timeout    60s;
        proxy_read_timeout    60s;
    }
}

# Nota: certbot --nginx va adauga automat blocurile server pentru HTTPS (port 443)
# cu ssl_certificate si ssl_certificate_key dupa prima rulare cu succes.
